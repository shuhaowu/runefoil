name: runefoil
image: ubuntu/xenial

users:
  - name: btw
    shell: /bin/bash

shell:
  user: root

shares:
  - source: .
    dest: /opt/runefoil

x11:
  enabled: yes

provisioning:
  - type: ansible
    playbook: playbook.yml
